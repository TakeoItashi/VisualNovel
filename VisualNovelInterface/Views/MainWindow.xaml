<Window x:Class="VisualNovelInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisualNovelInterface"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:TestViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File"/>
            <MenuItem Header="_Edit"/>
            <MenuItem Header="_View"/>
            <MenuItem Header="_Project"/>
            <MenuItem Header="_Help"/>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    BorderBrush="Gray" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0"
                            BorderBrush="Gray" BorderThickness="0,0,0,1">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0"
                                    BorderBrush="Gray" BorderThickness="0,0,1,0">
                                <TabControl>
                                    <TabItem Header="Story Tree">
                                        <Grid>
                                            <ListBox ItemsSource="{Binding CurrentProject.Panels}" DataContext="{Binding Mode=OneWay}" SelectedIndex="0" SelectedItem="{Binding SelectedPanel}">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding PanelName}"/>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Grid>
                                    </TabItem>
                                    <TabItem Header="Panel View">
                                        <Grid>
                                            <ListBox ItemsSource="{Binding SelectedPanel.DialogueLines}" SelectedIndex="0" SelectedItem="{Binding SelectedLine}" >
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding CharacterName}" />
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Grid>
                                    </TabItem>
                                </TabControl>
                            </Border>
                            <Grid Grid.Column="1">
                                <Image Source="{Binding SelectedPanel.BackgroundImage}" VerticalAlignment="Center" />
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="10*"/>
                                        <RowDefinition />
                                        <RowDefinition Height="4*"/>
                                    </Grid.RowDefinitions>
                                    <Rectangle Grid.Row="1" Margin="10,0,0,0" Width="60" Stroke="#FFC12F2F" Fill="#B2C12F2F" StrokeThickness="3" HorizontalAlignment="Left"/>
                                    <TextBlock Grid.Row="1" Margin="10,0,0,0" Text="{Binding SelectedLine.CharacterName}"/>
                                    <Rectangle Grid.Row="2" Margin="10,5,10,10" Stroke="#FFC12F2F" Fill="#B2C12F2F" StrokeThickness="3"/>
                                    <TextBlock Grid.Row="2" Margin="10,5,10,10" Text="{Binding SelectedLine.TextShown}"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Border>
                    <Grid Grid.Row="1">
                        <DockPanel>
                            <DockPanel DockPanel.Dock="Top">
                                <TextBox Margin="10,5,0,5" DockPanel.Dock="Left" HorizontalAlignment="Left" MinWidth="60" Text="{Binding SelectedLine.CharacterName, UpdateSourceTrigger=PropertyChanged}"/>
                                <Label Content=":"/>
                            </DockPanel>
                            <TextBox Margin="10,0,10,10" Text="{Binding SelectedLine.TextShown, UpdateSourceTrigger=PropertyChanged}" >

                            </TextBox>
                        </DockPanel>
                    </Grid>
                </Grid>
            </Border>
            <Grid Grid.Column="1">
                <StackPanel Margin="10">
                    <Button Content="Add Background Image" Command="{Binding AddBackgroundImageCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>